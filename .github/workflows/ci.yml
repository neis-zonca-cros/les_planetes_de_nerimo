name: 🚀 CI Pipeline Nérimo

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
      - develop

jobs:
  back_nerimo:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20]

    steps:
      - name: 📂 Check du code
        uses: actions/checkout@v2

      - name: 📥 Installation Node
        uses: actions/setup-node@v2
        with:
          node-version: ${{ matrix.node-version }}

      - name: 📄 Création d'un fichier .env.test dans back_nerimo
        working-directory: back_nerimo
        run: |
          echo "MONGODB_URI=${{ secrets.MONGODB_URI }}" >> .env.test
          echo "NODE_ENV=test" >> .env.test
          echo "PORT=3001" >> .env.test
          echo "JWT_SECRET=${{ secrets.JWT_SECRET }}" >> .env.test

      - name: ⚙️ Installation des dépendances
        working-directory: back_nerimo
        run: |
          npm install
          npm i npm-run-all

      - name: 🧪 Lancement des tests
        working-directory: back_nerimo
        run: npm run test

      - name: 🔍 Lancement des Linters
        working-directory: back_nerimo
        run: npm run lint


  front_nerimo:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Check du code
        uses: actions/checkout@v2

      - name: 📥 Installation Node
        uses: actions/setup-node@v2
        with:
          node-version: 20

      - name: ⚙️ Installation des dépendances
        working-directory: front_nerimo
        run: |
          npm install

      - name: 🔍 Installation et lancement des Linters en front_nerimo
        run: |
          npm run lint
          npm run prettier
